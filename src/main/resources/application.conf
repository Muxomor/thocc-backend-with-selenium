ktor {
  deployment {
    port = 7895
    watch = [classes]
  }
  application {
    modules = [ com.thocc.ApplicationKt.module ]
  }
  db {
      url = ${DB_URL}
      user = ${DB_USER}
      password = ${DB_PASSWORD}
  }

  telegram {
      token = ${BOT_TOKEN}
      chatId = ${CHAT_ID}
  }
}