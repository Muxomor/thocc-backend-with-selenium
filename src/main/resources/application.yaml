ktor:
    application:
        modules:
            - com.thocc.ApplicationKt.module
    deployment:
        port: 7895
db:
    url: ${DB_URL}
    user: ${DB_USER}
    password: ${DB_PASSWORD}

telegram:
    token: ${BOT_TOKEN}
    chatId: ${TELEGRAM_CHAT_ID}